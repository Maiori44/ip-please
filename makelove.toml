name = "ip-please"
default_targets = ["win32", "win64", "macos"]
build_directory = "release"

love_files = [
    "*.lua",
    "*.ttf",
    "*.txt",
    "*.glsl",
]

[hooks]
prebuild = [
    "clue src -t=luajit"
]
